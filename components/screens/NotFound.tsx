import { Center, Text } from '@components/core'
import { View } from 'react-native'

const NotFound = () => (
	<View style={{ flex: 1 }}>
		<Center>
			<Text>Not Found</Text>
		</Center>
	</View>
)

export default NotFound
