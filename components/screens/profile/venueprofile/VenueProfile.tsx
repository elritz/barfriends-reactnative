import { Text } from '@components/core'
import { View } from 'react-native'

enum ProfileType {
	USER = 'USER',
	VENUE = 'VENUE',
}

const VenueProfile = () => (
	<View>
		<Text>HELLO</Text>{' '}
	</View>
)

export default VenueProfile
