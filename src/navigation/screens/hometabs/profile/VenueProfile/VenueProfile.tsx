import { Text, View } from 'native-base'

enum ProfileType {
	USER = 'USER',
	VENUE = 'VENUE',
}

const VenueProfile = () => (
	<View>
		<Text>HELLO</Text>{' '}
	</View>
)

export default VenueProfile
